-r requirements.ui.txt

# local audio/testing only — DO NOT ship to Fly
pygame>=2.5,<2.6               ; platform_system == "Darwin"
sounddevice>=0.4,<0.5           ; platform_system == "Darwin"
pydub>=0.25,<0.26               ; platform_system == "Darwin"
ffmpeg-python>=0.2,<0.3         ; platform_system == "Darwin"

pytest>=8,<9
pytest-asyncio>=0.23,<0.24